# Optional: enables real LLM-powered diagnosis via Groq's free-tier API
# (Llama 3.3 70B, sub-second inference on Groq's LPU hardware).
# Get a free key at https://console.groq.com/keys
# Without this set, ZENITH runs fully offline using its deterministic
# rule-based fallback classifier -- no functionality is lost, only the
# reasoning quality of the diagnosis step.
GROQ_API_KEY=
